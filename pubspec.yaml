name: fond_check
description: "FondCheck: A Flutter desktop application for managing and viewing electronic prescriptions."

# Updated version to reflect 1.0.0 as per semantic versioning
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  file_picker: ^8.1.7
  flutter:
    sdk: flutter
  flutter_datetime_picker: ^1.5.1
  http: ^1.2.2
  intl: ^0.20.1
  shared_preferences: ^2.3.4
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
